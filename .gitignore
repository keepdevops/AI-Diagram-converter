# ---- Node / React (web-react, web) ----
node_modules/
dist/
.vite/
*.local

# ---- Python (diagram_agent, matrix_client, plantuml-editor) ----
__pycache__/
*.py[cod]
*$py.class
.venv/
venv/
env/
.Python
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# ---- Environment / secrets ----
.env
.env.*
!.env.example

# ---- Logs ----
*.log
scripts/logs/

# ---- Large / downloadable artifacts ----
# NOTE: plantuml.jar (~28MB, v1.2026.6) is intentionally TRACKED — the `bridge`
# render module builds it into the bridge image (docker/bridge-jar.Dockerfile) so
# the stack is reproducible fully offline from this repo alone. Un-ignored on purpose.

# ---- Editor / OS ----
.DS_Store
.idea/
.vscode/
*.swp
Thumbs.db

# ---- PID files / image tarballs ----
scripts/.pids/
*.tar

# ---- Nested repo (its own .git; not a submodule here) ----
AI-Diagram-converter/
